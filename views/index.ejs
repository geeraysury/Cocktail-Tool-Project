<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        Cocktail Result
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #213456;
            margin: 0;
            padding: 0;
        }

        form {
            margin-top: 20px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input[type="text"]{
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            margin-bottom: 10px;
        }

        .drink-name{
            text-align: center;
        }
        .cocktailImage{
            display: block;
            margin-left: auto;
            margin-right: auto;
            width: 30%;
        }
        input[type="submit"] {
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
        }

        .response-area {
            margin-top: 20px;
            background-color: #f0f0f0;

            padding: 20px 5px;
        }

        #ok {
            background-color:#FFB6C1;
        }
        #ok:hover {
            background-color: #b5606c;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>The Cocktail Dictionary</h1>
        
        <!--COCKTAIL NAME INPUT-->
        <form id="myForm" method="POST" action ="/search-cocktail">
            <label for="cocktailNameInput">Search Cocktail Name:</label>
            <input type="text" id="cocktailNameInput" name="cocktailName">

            <input id="ok" type="submit" value="Search">
        </form>

        <h3>OR</h3>

        <!--ALCOHOL NAME INPUT-->
        <form id="myForm" method="POST" action="/search-alcohol">
            <label for="alcNameInput">Search Alcohol Name:</label>
            <input type="text" id="alcNameInput" name="alcName">

            <input id="ok" type="submit" value="Search">
        </form>
        <div class="response-area">
            <!--COCKTAIL RESULT-->
            <% if(error) { %>
                <h2><No result found></h2>
            <% } else if (drinkName) { %>
                <h2 class="drink-name">
                    <%=drinkName %>
                </h2>
                <!--HANDLE IMG ERROR (IF DRINK NOT FOUND)!!!!!!-->
                <% if (image) { %> <!-- Check if image exists -->
                    <img src="<%= image %>/preview" class="cocktailImage" alt="Image of <%= drinkName %>">
                <% } %> 
                
                <h3>Ingredients:</h3>
                <ul class="ingredients">
                    <% for(var i=0; i < ingredients.length; i++){ %>
                        <li>
                            <%= ingredients[i].measure %>  <%= ingredients[i].ingredient %>
                        </li>
                    <%}%>
                </ul>
                <h3 class="instructions">
                    Instructions:
                </h3>
                <ol>
                    <% for(var i = 0; i < instructionsSteps.length; i++) { %>
                        <li><%= instructionsSteps[i].trim() %>.</li> <!-- Trim any excess space and add a period -->
                    <% } %>
                </ol>
                

                <%if (locals.error) {%>
                    <div id="tag-error" class="tag-error">
                        <%=error%>
                    </div>
                <%}%> 
            <% } else if (alcName) { %>
                <!--ALCOHOL RESULT-->
                <h2 class="alc-name">
                    Alcohol: <%= data.strIngredient %> <!--was ingredient-->
                </h2>
                <p>Description: <%= data.strDescription %></p> <!--was description-->
            <% } %>
            <% if (locals.error) { %>
                <div id="tag-error" class="tag-error">
                    <%= error %>
                </div>
            <% } %>
        </div>     
    </div>
</body>

</html>